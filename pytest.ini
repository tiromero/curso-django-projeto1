[pytest]
DJGANGO_SETTINGS_MODULE = project_hub.settings 
python_files = test.py tests_py test_*.py tests_*.py *_test.py *_tests.py 
addopts = 
    --doctest-modules
    --strict-markers
markers =
    slow: Run tests that are slow
    fast: Run fast tests